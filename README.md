# Projects Manager Back-end
This project uses Node JS v12.14 and Express.

## Available Scripts

In the project directory, you can run:
### `npm install`

Run project locally:
### `npm start`

Build:
### `npm run build`
